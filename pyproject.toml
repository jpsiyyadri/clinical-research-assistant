[project]
name = "assistant-research-hypothesis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.6",
    "pydantic>=2.10.3",
    "ruff>=0.8.2",
]
